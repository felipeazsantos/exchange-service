server:
  port: 8000

spring:
  application:
    name: exchange-service
  datasource:
    url: jdbc:mysql://localhost:3306/exchange?allowPublicKeyRetrieval=true&useSSL=false
    username: exchange
    password: exchange
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

